spring:
  config:
    activate:
      on-profile: alpha
logging:
  level:
    root: DEBUG

---
spring:
  config:
    activate:
      on-profile: beta
logging:
  level:
    root: INFO

---
spring:
  config:
    activate:
      on-profile: perf
logging:
  level:
    root: OFF
    io.dave.shorter.adapter.outbound.event: OFF
